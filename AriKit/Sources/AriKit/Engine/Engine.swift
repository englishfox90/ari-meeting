///
///  Engine.swift — SCAFFOLD, no engine code yet, gated by Phase 3.
///
///  Per plans/swift-migration-plan.md, this module will hold the ported capture/STT/
///  summarization pipeline (or its Swift-side orchestration of the sidecars) once the
///  audio and LLM subsystems move off the frozen Rust/Tauri engine — the Swift mirror of
///  today's Rust `audio/`, `whisper_engine/`, `parakeet_engine/`, and `summary/` modules.
///
public enum Engine {}
