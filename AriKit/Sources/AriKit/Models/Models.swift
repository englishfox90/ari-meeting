//
//  Models.swift — SCAFFOLD, no engine code yet, gated by Phase 3.
//
//  Per plans/swift-migration-plan.md ("Target architecture"), this module will hold the
//  shared domain value types: meetings, transcripts, summaries, persons, meeting series,
//  and profile facts (with provenance/supersession) — the Swift mirror of today's Rust
//  `database::models`. Real types land phase by phase, gated by the Phase-0 spikes.
//
public enum Models {}
