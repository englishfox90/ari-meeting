# Swift-Native Migration Plan

**Status: 2026-07-21.** This section is the single source of current truth; the phase sections below carry per-phase detail, and a condensed **Revision history** is at the end.

## Where we are now

- **Phase 0 (de-risk spikes) — COMPLETE / GO.** S1 GO (MLX Qwen3.5-4B), S2 GO-with-caveats (SpeechTranscriber), S3 conditional-GO (FluidAudio + calendar-derived speaker-count hint; sherpa fallback; formal close pending one clean 3+ speaker recording — non-blocking, needed before Phase 3.5). Eval rigs committed (P0). S4's local-store half folds into the Phase-3 store port; the CloudKit half waits for Phase 5.5.
- **Phase 1.5 (headless `ari-engine` carve) — Stage B1 COMPLETE.** The `ari-engine` crate holds the entire engine **brain**: config, models, database, all providers (+ollama), apple, summary (+summary_engine), whisper_engine, embed_models, diarization, recall (safety-shell + `local_recall_tests` extracted from `api.rs`, reviewer-verified), meeting_series, persons, language_preference, and the `Engine`/`EventSink`/`Notifier`/`Paths` hub — across dependency-ordered, individually-reviewed green commits (`03c7e0f`→`1f1e528`). **Host stays thin by design:** the OS/UI/STT edges — `calendar` (EventKit), `notch` (host UI), `audio` capture (cpal/cidre/ScreenCaptureKit), `parakeet_engine`/`whisper_engine` (STT, replaced by SpeechAnalyzer not ported), tray, TCC — remain host. **Stages C/D (method router + NDJSON daemon) are DEFERRED** (native-first won: the Phase-0 spikes greenlit Swift-native STT/summary/diarization, so we build the daemon later and narrowly, only for anything Swift genuinely can't yet do). Detail: `docs/plans/ari-engine-carve.md`; deferred protocol/service map: `docs/plans/engine-extraction.md`.
- **Phase 3.1 AriKit Store — COMPLETE (plain GRDB).** Four reviewed green slices (`e4c2a0c`→`b956e99`): the full `v1_baseline` schema (15 tables, parent-before-child FK order, soft-delete tombstones), internal records + `Sendable` value-in/value-out repositories (repositories-only), the additive `Summary` + `MeetingNote` domain types, persons/facts two-tier (provenance + read-time `sourceCount`, repository-enforced single owner), series (3-table reconciliation) + calendar (attendees inline JSON), and the data-preservation centerpiece — a one-time, read-only, idempotent **legacy-data importer** from `com.meetily.ai/meeting_minutes.db` (audio-by-reference, honest `ImportReport` reconciliation, `meeting_notes` preserved) that passed a `swift-code-reviewer` data-fidelity gate (a HIGH reconciliation hole + two MEDIUMs fixed before commit) + `SingleOwnerTests`. The snake→camel decode adapter (store step 8) is **deferred** (no consumer until the engine-IPC bridge). Plan: `docs/plans/arikit-store.md`.
- **Phase 3.1 AriKit Recall — Slice 1 landed** (`033955f`). The pure domain layer — safety shell (loopback policy, context/history bounding, question-scope, prompt, wire types), citations ([S1] verification + @ref(MM:SS) filtering), chunker, vector math — with **every Rust `#[cfg(test)]` ported 1:1 as Swift Testing**, so the recall invariants (loopback-only, bounded context, never-invents-citations, @ref filtering, out-of-scope refusal) survive as tested Swift behavior (principle 6). Zero Store/sidecar/LLM dependency. Plan: `docs/plans/arikit-recall.md`.
- **AriKit package state:** Models ✅, Store ✅, Recall (Slices 1–8) ✅ — full safety shell + index + embedder + hybrid search + indexer + Ask store + people context + **answerable orchestrator (single-shot + streaming; agentic loop deferred)**. Engine ✅ **Phase 3.4 provider layer (A–D: `LLMClient` protocol + OpenAI-compatible/Anthropic/Claude-CLI/FoundationModels) + summary pipeline (F) + summary service (G)**, **Phase 3.3 STT (COMPLETE 2026-07-20)** — the capture-agnostic `Engine/STT/` layer on SpeechAnalyzer/SpeechTranscriber (segment-emitting `TranscriptionProvider`, `SpeechAssetManager`, `TranscriptMapping`, file + designed-live paths); **Phase 3.4 Track E MLX (LANDED 2026-07-20, mechanism-GO)** — `AriKitEngineMLX` (separate product; `MLXClient`+`ModelHost`, true streaming, `enable_thinking:false`); and **Phase 3.4 Track H Persons (LANDED 2026-07-20)** — `Engine/Persons/` extraction + reconciliation. **Capture ✅ Phase 3.2 Slice S1 (LANDED 2026-07-20)** — `AriCapture` macOS-only product (pure-DSP core: `PCMWindow` seam, Resampler, AudioMixer, VAD segmenter, AACRecorder, IncrementalSaver). Series (3.4 Track I) still deferred (Phase-2-blocked); the MLX numeric 3-axis gate + capture device slices (3.2 S2–S5) still open — see Current front. DesignSystem ✅ (Marginalia tokens + parity test + **button system** `MarginaliaButtonStyle`, 4 roles × 2 sizes); Context is a scaffold stub. **"Ask my meetings" is now answerable end-to-end in Swift** (cloud/Ollama/Claude-CLI/Apple). **Phase 2 native shell UNDERWAY (2026-07-20):** `Ari/` Xcode app target (bundle `com.arivo.ari`) — **S0 app skeleton ✅**, **S8-lite first-run legacy import ✅** (adopts the live `com.meetily.ai` library read-only; 15 meetings / 3081 transcripts / 39 persons / 14 summaries verified), and the **S6 native read UI ✅** (3-column `NavigationSplitView` over Meetings/People/Series + meeting detail with transcript/summary/notes + native `AVPlayer`), built on a new **`AriViewModels`** package product (headless-testable `@Observable`-MVVM). Read-UI-first sequencing (S0 → import → S6 → capture vertical), reviewed + all findings fixed. **Combined suite: 582 tests / 96 suites, 0 warnings, Swift 6 strict** (2026-07-21, after the read-UI polish — see the Phase 2 PROGRESS note).
- **Store library decision (revised 2026-07-17):** **plain GRDB now**, revisit SQLiteData at Phase 5.5 (SwiftData stays permanently rejected). See Decisions + `docs/plans/arikit-store.md §0.1(3)`.

### Current front & what's next

**Recall Slices 1–8 are landed** (safety shell; index schema FTS5 ⊕ vector; Apple `NLEmbedding` embedder; hybrid RRF search; idempotent indexer; Ask conversation store; partial people context; **the answerable orchestrator — single-shot + streaming**). **Phase 3.4 Engine is landed** through the summary spine: the `LLMClient` provider layer (OpenAI-compatible/Anthropic/Claude-CLI/FoundationModels), the summary pipeline (templates/chunking/citations), and the summary service — so **"Ask my meetings" now answers end-to-end in Swift** on cloud/Ollama/Claude-CLI/Apple. **Phase 3.3 STT is landed** (COMPLETE 2026-07-20): the capture-agnostic `Engine/STT/` SpeechAnalyzer/SpeechTranscriber port passed its dual-run gate — the product `SpeechTranscriberProvider` scored **mean core WER 0.2345 vs the Parakeet baseline 0.2814** over the 5 S2 held-out meetings (reproducing the S2 spike ~0.234), 100% word-timestamp coverage, punctuation on every meeting, longest real file (~45 min) transcribed single-pass with no truncation. WhisperKit deferred (protocol kept backend-ready). Plan: `docs/plans/arikit-stt.md`.

**LANDED 2026-07-20 (parallel Sonnet-implementer worktrees, Opus-reviewed + gated, pushed to `main` `731a2d7`):** three tracks — **Phase 3.2 Slice S1** (`AriCapture` pure-DSP core, per `docs/plans/arikit-native-shell.md` §11), **Track E MLX** and **Track H Persons** (both per `docs/plans/arikit-engine-extras.md`). Combined **506 tests / 76 suites** green; a `swift-code-reviewer` HIGH (ModelHost async-cache reentrancy → duplicate model downloads) was fixed. The **MLX gate is mechanism-GO, not full numeric-GO**: real Qwen3.5-4B-MLX-4bit inference passes on the product path (no `<think>` leak, true streaming, honest `providerUnavailable`) via `xcrun xctest` on the xcodebuild-built bundle (bare `swift test` has no metallib — must use the xcodebuild lane); the full 3-axis meet-or-beat scoring (`tools/prompt-harness/compare.mjs`) is **not yet ported to Swift**, so those numbers remain inherited from the S1 spike. Bundle id confirmed **`com.arivo.ari`**.

**Done since (2026-07-20 → 2026-07-21):** **Phase 2 S0 app skeleton + S8-lite legacy import + S6 native read UI** landed and reviewed (see the package-state bullet above), and on top of that the **shell/navigation + Marginalia design arc has now landed** across eight reviewed commits (`b8e4dd9`→`1f623d1`): the **Marginalia app icon + shell/nav branding** (`104f9be`), a **Home landing screen + rich left-nav rail** in the old-app layout with the **Bricolage Grotesque** display face (`2fc3ab5`, refined Apple-Music-inspired in `ea7f6f3`; ExtraBold-render fix shipping static SemiBold/Bold cuts in `1f623d1`), the **Marginalia component library + design gallery** (`58aecf8`) with its Fable design-review findings actioned (`b7bb327`), **macOS 26 Liquid Glass on the chrome/action layer** (`ac4e0bf`), and shared **`CardListScaffold` + `TranscriptSegmentRow`** extraction (`70131c0`). So the app icon/branding and shell/navigation items that were the prior "current focus" are **done**. New sub-plans: `docs/plans/arikit-native-read-ui.md`, `docs/plans/arikit-component-library.md`, `docs/plans/liquid-glass-adoption.md`, `docs/plans/marginalia-review-fixes.md`. **Current focus: the meeting-detail read experience** — a redesign of the detail pane (listen-back panel, referenced-moments bar, source-record panel, native Marginalia markdown rendering, per-series summaries) is **in flight in the working tree, not yet committed**.

**Still deferred / what's next:** the **MLX numeric 3-axis gate** (port `compare.mjs` scoring to Swift, or pipe MLXClient outputs through the Node scorer) before `.mlx` is a true numeric-GO; **capture device slices 3.2 S2–S5** (SystemAudioTap / MicrophoneCapture / CaptureCoordinator / capture→STT join) — need a signed `.app` + a one-time human TCC grant, so they are not agent-closeable; **S7 EventKit calendar** (also TCC-gated); **Track I Series** (Phase-2-blocked on EventKit-synced calendar rows); the Claude agentic tool-use loop (recall follow-on); and **diarization (3.5)**. Then the rest of **Phase 4** UI (block editor). Cloud/sync stay parked behind Phase 5.5; mobile is Phase 6. Gated plans: `docs/plans/arikit-native-shell.md` (Phase 2 shell + 3.2 capture — native-first/no-WebView-bridge) and `docs/plans/arikit-engine-extras.md` (3.4 MLX/persons/series — order E→H→(Phase 2)→I).

> **Coordination note:** work-streams commit to `main` in parallel — keep to the principle 8 WIP limit and give each parallel implementer its **own git worktree** (they merge back), since a shared working tree between concurrent sessions is fragile (2026-07-20: a separate concurrent job's uncommitted `Ari/` skeleton + `arikit-native-shell.md` edit coexisted in the tree during the S1/H/E landing).

A staged plan to migrate Ari from its current Rust/Tauri + Next.js polyglot stack to a **100% Swift, Apple-only** codebase — and, in doing so, lay the foundation for an Apple multi-device product family. The **macOS app is migrated first and finished first**; a mobile ("lite") app on iOS/iPadOS is a **separate, later project built on the finished `AriKit`** once the Mac migration is complete (see Phase 6). "Lite" does **not** mean read-only: the mobile app aims for **the same feature set as the Mac app minus speaker identification** (F1) — recording, transcription, and summarization run on-device there too — because on-device speaker diarization/re-ID has no proven mobile model yet (revisit if one is found). Text results sync across devices through iCloud; audio stays on the device that recorded it.

Companion docs: `plans/leverage-apple-models.md`, `plans/diarization-production-plan.md`, `.claude/context/architecture.md`, `meeting-intelligence-prd.md`. **Per-subsystem AriKit plans:** `docs/plans/arikit-store.md`, `docs/plans/arikit-recall.md`, `docs/plans/arikit-engine-providers.md` (Phase 3.4 provider+summary), `docs/plans/arikit-engine-extras.md` (Phase 3.4 deferred — MLX/persons/series, gated), `docs/plans/arikit-native-shell.md` (Phase 2 shell + Phase 3.2 capture, gated), `docs/plans/arikit-native-read-ui.md` (Phase 2 S6 read UI), `docs/plans/arikit-component-library.md` + `docs/plans/liquid-glass-adoption.md` + `docs/plans/marginalia-review-fixes.md` (Phase 2 Marginalia design arc).

## Verdict

**Migrate — but as a strangler, not a big-bang rewrite.** The decisive reasons are Apple-only intent + the eventual iCloud/mobile-app capability, which the current stack is structurally bad at (no CloudKit from Rust; the heavy engine — a Rust/C++ polyglot — can't be shared with a native iOS app anyway). Most of the C/C++ engine surface *dissolves* into native frameworks (MLX, SpeechAnalyzer, Core Audio process taps, AVAudioEngine, AVFoundation, GRDB) rather than needing re-wrapping — the notable exception, diarization/re-ID, already has a scoped Swift path (**FluidAudio**, CoreML pyannote, offline DER ~10.6% on AMI).

The multiplier: **we have a ~90%-finished product**. This is a second-system rewrite *with the full spec already in hand* — every behavior, edge case, and quality bar is known and encoded in working code and tests. That removes the usual rewrite risk (discovering requirements late) and lets us hold each Swift component to a measured bar before it replaces its predecessor.

**Honest scope numbers** (measured 2026-07-16): the Rust engine is **~62k lines** (196 files) — *larger* than the **~42k-line** React/BlockNote UI (243 files). "Dissolves into frameworks" is an argument about *effort*, not lines: much of that Rust exists to bridge APIs Swift calls natively. But the engine port is the bulk of the calendar time, and this plan quantifies it (see the subsystem map) instead of hand-waving it.

The two things a rewrite does **not** make cheaper, and where the real months go:
1. The **~42k-line React UI** — specifically its *block-editor* layer. (Correction from v1: as of macOS/iOS 26, SwiftUI `TextEditor` binds `AttributedString` natively with selection + formatting — plain rich text is no longer hard. What has no native equivalent is BlockNote-style **block editing**: drag handles, slash menus, and our ProseMirror `@ref` badge decorations. That layer migrates **last**, possibly living in a scoped WebView longest.)
2. **Diarization/re-ID** — a real model-port effort in any language.

## Guiding principles

1. **Strangler, never big-bang.** The current app stays shippable and usable the entire way. We stand up a native shell and replace pieces behind it; nothing is deleted until its replacement beats it.
2. **Quality gates before commit, dual-run first.** No engine component is swapped until the Swift version **meets or beats** the incumbent on a fixed eval set. For each port: the invariant/acceptance suite is written (or committed) *first*, run green against the **Rust incumbent**, then against the Swift candidate. ✅ Prerequisite met (2026-07-16/17): the eval rigs are committed — `tools/prompt-harness/` (S1), `tools/stt-eval/` (S2), and `tools/diarization-sweep/` (S3) — so every gate has a measuring stick. The Swift-side spikes live in `spikes/{mlx-swift-s1,speechanalyzer-s2,fluidaudio-s3}/`.
3. **Exactly one process owns the database at any time.** Everyone else (sidecars, the Swift shell before cutover, the CloudKit publisher) goes through the owner's API. No cross-process dual-ORM WAL writes (sqlx + GRDB on the same file is a lock-contention/corruption footgun, and sqlx's `_sqlx_migrations` table is invisible to GRDB). CloudKit sync cursors/state belong to the DB owner too.
4. **Mac first, then sync, then mobile — but schema-sync-aware from the start.** *(Revised 2026-07-16 — Paul: iCloud sync moves to **after the full Mac app is done** and **before** the mobile app; no dev account is bought until then. Supersedes the earlier "build the sync layer in Phase 1" sequencing.)* The macOS Swift migration runs to completion first (Phases 1.5–5). Then the **sync machinery** is turned on in a new **Phase 5.5** (get the Apple Developer account, create the iCloud container, wire SQLiteData's CloudKit sync, promote spike S4 to real infra, backfill existing rows). Then the mobile app is a separate, later project on the finished `AriKit` (Phase 6). **What must NOT be deferred is *schema-sync-awareness*** — the distinction is: the sync *machinery* (account, container, sync engine, S4, conflict testing) is cheap to defer and touches no data; a small set of *schema-shape* decisions are cheap now but a data-migration-on-live-data if retrofitted after the Mac has real meetings in it. The store built in Phase 3 is therefore **sync-aware but sync-off**, honoring the four constraints in the "Sync-aware schema" checklist below (§ Phase 3). This lets Paul stay fully local — no account, no cloud — while keeping Phase 5.5 a clean *switch-on* rather than a schema surgery. Sync also has standalone Mac-to-Mac / backup value, so it earns its place even independent of the mobile question.
5. **Sync text, keep audio local.** Transcripts/summaries/metadata are small and sync freely via CloudKit; meeting audio is large (we've hit 138 MB files) and stays on the device that recorded it (fetched on demand as a `CKAsset` only if ever needed). Since the mobile app records on-device too, "local" means device-local on both Mac and phone — sync is bidirectional for text, never for audio.
6. **Preserve the invariants.** The recall safety shell (loopback-only local path, bounded context, never-invents-citations — enforced by the existing `recall/` unit tests), consent-before-record, and the design system's No-Fake-State rule survive the port verbatim, as ported Swift test suites (swift-testing/XCTest), not just intentions.
7. **Latest-OS-only is an accepted constraint.** SpeechAnalyzer + FoundationModels are macOS/iOS 26+; Core Audio process taps need macOS 14.4+; MLX needs recent Apple Silicon. For a private single-user product this is fine — but it's a hard floor we commit to openly.
8. **WIP limits — the strangled tree must not outgrow the strangler.** At most **one migration phase active** at a time, and at most **one product feature in flight** concurrently — and that feature lands on the *target* side of any seam already cut (once the Swift store exists, new tables go into GRDB only; once the shell exists, new UI goes SwiftUI-first). Features still deep in Rust (F1–F4) get finished on the current stack, then the Rust engine freezes except bugfixes; F5/F8-class net-new work goes Swift-native.

## Target architecture (end state)

A single **Swift Package** of shared domain code, consumed by two app targets. **The macOS target is built and finished first; the iOS target (Phase 6) reuses as much of the shared engine as the platform allows — everything except speaker ID.**

```
AriKit  (shared Swift package)
├── Models        meetings, transcripts, summaries, persons, series, profile facts
├── Store         GRDB (local source of truth)  +  CloudKit sync (results layer)
├── Recall        hybrid retrieval (BM25 ⊕ vector RRF), safety shell + tests preserved
├── Context       SummaryContext assembly (owner + attendees + call type)
└── Engine        capture-agnostic STT / summary / persons / series / recall
                  (shared by both targets; diarization is macOS-only)

Ari (macOS app target — FIRST)               Ari Lite (iOS / iPadOS target — LATER, Phase 6)
├── Capture   Core Audio process tap (sys) + ├── Capture   AVAudioEngine mic
│             AVAudioEngine (mic)            │             (no system-audio process tap on iOS —
├── Encode    AVFoundation (AAC), replaces   │             see open platform deltas below)
│             the ffmpeg sidecar             ├── Encode    AVFoundation (AAC), shared
├── STT       SpeechAnalyzer / WhisperKit    ├── STT       SpeechAnalyzer / WhisperKit (shared)
├── Diarize   FluidAudio (CoreML, offline)   ├── Diarize   ✗ none (no proven mobile model yet)
├── Summary   MLX (Qwen-4B / Gemma-E4B) /    ├── Summary   MLX Gemma-E2B/E4B (128K) /
│             cloud / Claude CLI; FM floor   │             cloud  (tiered to device RAM)
├── Calendar  EventKit                       ├── Calendar  EventKit
├── Notch     (ari-notch UI absorbed)        ├── Recall / persons / series (shared)
└── SwiftUI UI (full)                        └── SwiftUI UI (full, minus speaker labels)
```

`#if os(macOS)` gates the **Mac-only** engine pieces — the system-audio process tap and diarization/re-ID — while the shared `AriKit.Engine` (STT, summary, persons, series, recall, context) compiles for both targets. Cloud providers (Anthropic/OpenAI/Groq/OpenRouter) port trivially over URLSession on both platforms; **exception: the Claude CLI provider is a subprocess wrapper around the local `claude` binary, not HTTP** — it ports as a `Process` spawn, and only on macOS (no subprocess spawning on iOS).

**Open platform deltas for the mobile app (resolved in Phase 6, flagged now so the shared engine is designed for them):**
- **System-audio capture — CONFIRMED macOS-only, not an open question (verified 2026-07-16, incl. WWDC 2026 / iOS 27).** The Core Audio process tap is macOS-only (introduced macOS 14.2; never ported to iOS), and iOS sandboxing forbids capturing other apps' audio output. **WWDC 2026 / iOS 27 did not change this** — the iOS 26 recording additions (input-device picker, `bluetoothHighQualityRecording`, simultaneous record+process) are all mic-side, and WWDC26's audio work (Music Understanding, NowPlaying) is analysis/playback, not capture. The *only* iOS path to another app's audio is a **Broadcast Upload Extension** (ReplayKit) — a separate process, ~50 MB memory cap, Control Center-driven UX — unsuitable for silently capturing the far side of a Zoom/Teams call. **Consequence:** the mobile app is **mic-first by hard constraint** — it captures in-room/local audio well, but cannot capture the remote side of a call the way the Mac's process tap does. This is a genuine feature gap *beyond* speaker ID. Re-check each WWDC for a new iOS capture API before Phase 6; absent one, scope the mobile app as mic-first (in-person meetings + the local speaker's side of calls). Ref: `capturing-system-audio-with-core-audio-taps` (Apple docs, macOS-only).
- **On-device summary model** — MLX runs on iOS with tighter memory than a Mac, but **Gemma 4 E2B (~1.5 GB @4-bit, 128K ctx) fits comfortably on phone** and E4B (~5 GB) on higher-end devices, so robust on-device mobile summarization is viable — not forced onto FoundationModels/cloud. Exact mobile default (E2B vs E4B vs cloud) tuned to real device memory at Phase 6; the tiered engine abstraction is built on the Mac side first.
- **Speaker ID** — excluded by design (no proven on-device mobile diarization model). Revisit if FluidAudio/CoreML pyannote is shown viable on an iPhone/iPad, or a lighter model appears.

## Phase 0 — De-risk spikes (go / no-go gate) — ✅ COMPLETE (2026-07-17): GO

Run these **before** committing to the migration. Each is a throwaway spike measured against real recordings from the existing SQLite DB. **Result: GO** — S1 GO (MLX Qwen3.5-4B), S2 GO-with-caveats (SpeechTranscriber), S3 conditional-GO (FluidAudio + calendar count hint; sherpa fallback; formal close deferred, non-blocking). Rigs committed (P0). The one residual is S4's local-store round-trip, which folds into Phase-3 step 3.1 rather than gating here.

**P0 (prerequisite): commit the eval rigs. ✅ DONE (2026-07-16/17).** The prompt-validation harness is committed at `tools/prompt-harness/`, the STT rig at `tools/stt-eval/`, and the diarization sweep (`run_sweep.py`, `postmerge.py`, `extract_reference.py`, `der.py`) at `tools/diarization-sweep/`, all reading the live SQLite DB read-only and the recordings under `~/Movies/meetily-recordings/`. S1–S3 all measured against them below.

| Spike | Question | Pass bar |
|---|---|---|
| **S1 — on-device summary** | Which on-device Swift model best matches the Qwen-4B GGUF baseline? Bake off the candidates through the committed prompt-harness (each is just a `run({system,user})→{text}` backend): **(a) MLX Qwen-class 4B** (same modeling we ship today, pulled into Swift); **(b) Gemma 4 E4B** (~4.5B eff, **128K ctx**, native **MLX-swift**, ~5 GB @4-bit) and **E2B** (~2.3B eff, 128K ctx, ~1.5 GB) — the mobile-grade models; **(c) FoundationModels** kept only as a zero-download fallback. **Key finding (2026-07-16):** FoundationModels' 4,096-token shared window (unchanged through iOS 26.4; Apple just throws on overflow) forces map-reduce, which provably sheds the citation/table/per-person granularity Ari depends on — whereas MLX-Qwen and Gemma-128K hold a full meeting in **one pass**. So the real contest is MLX-Qwen vs Gemma-E4B on quality; FoundationModels is a floor, not a contender for the default. | Summaries rated ≥ current on the prompt-harness set (**n=9** real transcripts — Paul accepted 9, no wait for a 10th; blind A/B) |
| **S2 — STT accuracy** | Does SpeechAnalyzer match Parakeet/Whisper-large on real meeting audio? Public benchmarks only show it beating Whisper-*Small*; parity with our Parakeet baseline is **genuinely unproven** — this gate is real, not a formality. Also verify: `.audioTimeRange` word timestamps (final results only), `SpeechDetector` VAD, and the ~30-locale limit vs Whisper's ~99. Fallback candidate: **WhisperKit** (CoreML Whisper — the proven Swift path; MLX-Whisper is not turnkey in Swift). **Correction (2026-07-16): the new framework DOES support a custom language model** — Apple's `RecognizingSpeechInLiveAudio` sample wires `DictationTranscriber(contentHints: [.customizedLanguage(modelConfiguration: SFSpeechLanguageModel.Configuration)])`, built via `SFSpeechLanguageModel.prepareCustomLanguageModel(...)`. So the earlier "no custom vocabulary" gap is **wrong**: SpeechAnalyzer accuracy on attendee names / domain jargon is *mitigable*, and uniquely so for Ari since we know the room (feed calendar/persons names + recurring terms as the custom LM). Consider a custom-LM S2 variant if the base run loses on names. | WER + punctuation ≥ current on ≥5 held-out meetings of mixed quality |
| **S3 — Diarization port** | Does FluidAudio (CoreML pyannote) hit the DER we get from the tuned sherpa-onnx recipe? Compare **offline pipelines only** — FluidAudio's offline DER is ~10.6% avg (AMI SDM) but its streaming modes are far worse (38–53%); do not gate on streaming. **Ground truth (decided 2026-07-16):** use *verified-correct current-app diarization* as the RTTM reference — extract (start, end, speaker) per transcript row from `meeting_minutes.sqlite`. This makes S3 a **parity-vs-current-shipping** test (the right migration bar), NOT absolute DER against human labels. **Seed reference:** meeting `meeting-d894f3ce-…` "Adhoc with Nia" (2 speakers, ~10 min, Paul confirms correct) — good for building/validating the plumbing, but it's the *easy* end. **Rig-build finding (2026-07-16): the primary metric is `stamp_accuracy`, not raw DER.** The app never adopts diarizer segment boundaries — `stamp_transcripts` (commands.rs) stamps existing transcript rows by max-overlap — so raw DER is inflated by dual-VAD boundary mismatch (the reference uses the transcript's Parakeet VAD; a fresh sherpa run does its own pyannote VAD). On Adhoc Nia a faithful sherpa re-run scored **DER 0.22 but stamp_accuracy 1.0000** (100% of reference speech gets the correct speaker) — zero real label disagreement. So gate FluidAudio on **stamp_accuracy** (what actually determines app output), with DER as a secondary diagnostic. | Primary: **`mean stamp_accuracy(fluidaudio) ≥ mean stamp_accuracy(sherpa)`** on the verified set; DER secondary. ≥ parity on the calibrated recordings **plus ≥3 fresh** — grow past the 2-speaker seed to 3+ speaker / remote-mixed cases before the verdict counts. |
| **S4 — CloudKit / store** | Confirm **SQLiteData** (the chosen store) as load-bearing: prototype a private-DB schema and round-trip a meeting result Mac→iCloud→iOS through SQLiteData's CloudKit sync (queued offline changes, record-level conflict resolution). Validate that raw-SQL recall (FTS5 / sqlite-vec) coexists with the synced tables. | Clean sync of text records; conflict handling sane; recall SQL runs against the synced DB |

**S2 spike result — GO-with-caveats (2026-07-16):** on 5 real meetings scored vs a Whisper-large-v3 pseudo-reference, **`SpeechTranscriber` beat Parakeet on every meeting** (mean core WER **0.234 vs 0.281**), plus **100% word-level `audioTimeRange` timestamps** (Parakeet is segment-only) and more punctuation. Adopt **SpeechTranscriber** for the STT port. Surprises that overturn assumptions: (1) **`DictationTranscriber` is WORSE (0.448), not better** — despite the name, it drops 35–45% of conversational content on long audio (tuned for short command dictation); don't use it as configured (try the `.progressiveLongDictation` preset before fully writing it off). (2) SpeechTranscriber **already emits punctuation** — not "raw words." (3) The **names/jargon gap did NOT materialize** ("Metro 2" etc. correct) — the custom-LM capability is a latent upside, not a required fix. Caveats: (a) long-file no-chunking only verified to ~45 min — a DB/audio quirk (two meetings' `MAX(audio_end_time)` is ~2× the real ~39–40 min file, a pre-existing app data-integrity bug worth its own look) meant a true 60–80 min single file was never exercised; test one before betting Phase-3 on no-chunking; (b) the Whisper reference hallucinated repetition loops on the 7-speaker crosstalk meeting (both real engines were right there), inflating that meeting's absolute WER but not the ranking. No OS/beta blocker. Rig: `tools/stt-eval/` + `spikes/speechanalyzer-s2/`.

**S3 spike result — CONDITIONAL GO, blocked on a clean reference (2026-07-17):** built the FluidAudio runner (`spikes/fluidaudio-s3/`, SwiftPM, FluidAudio **0.15.5**, offline API `OfflineDiarizerManager.process(audio:)` → `DiarizationResult.segments`; `swift build -c release`, no Metal step; CoreML models auto-cache to `~/Library/Application Support/FluidAudio/Models/` ~21 MB; a 40-min meeting diarizes in **~8 s** vs sherpa's minutes on CPU-ONNX — roughly **20–60× faster**, ANE-accelerated, and it deletes the Rust `diarize-helper` sidecar). Scored through the rig's `der.py --engine fluidaudio`. **Results, three findings:** (1) On the verified 2-speaker seed, FluidAudio best = **stamp_acc 0.961** (after a speech-time floor removes a spurious 2.3 s singleton) vs sherpa 1.000 — but the reference *is* sherpa's own output, so sherpa's 1.000 is **circular**; the honest read is FluidAudio independently agrees with the human-verified labels ~96%. (2) At **default auto-speaker-count**, FluidAudio **collapses both multi-speaker meetings to a single speaker** (stamp_acc 0.33 / 0.14) — a disqualifying failure, unmoved by the clustering threshold. (3) **But the collapse is an auto-COUNT failure, not an inability to separate voices:** FluidAudio's `clustering.numSpeakers/minSpeakers/maxSpeakers` accept a count hint, and feeding the known attendee count flips it to **near-parity with sherpa** — PM-Jul14 0.327→**0.716** (6 spk), PM-Jul7 0.143→**0.534** (5 spk, essentially tied with sherpa's 0.546). **This is exactly the PRD F4→F1 design** (calendar attendee list constrains the label space for mixed/remote audio); the one input FluidAudio needs, the product already supplies via EventKit. **Verdict: adopt FluidAudio as the diarization target, driven by the calendar attendee count, with the sherpa sidecar kept callable as fallback until parity is confirmed on a clean set.** ⚠️ **Formal close is still blocked:** every reference we have is sherpa-derived (circular), the two natural multi-speaker meetings are `source:"import"` mixed single-track audio, and two others carry the 2× `audio_end_time` DB bug — so there is **zero human-verified 3+ speaker reference**. Closing S3 needs one clean, human-confirmed 3+ speaker recording (ideally in-person/native, since a remote meeting is one mixed system stream and is unseparable by design). Rigs: `spikes/fluidaudio-s3/` + `tools/diarization-sweep/`.

**S1 spike result — early GO (2026-07-16):** a throwaway Swift runtime spike (`spikes/mlx-swift-s1/`) proved the actual ship path works. **`mlx-swift-lm` 3.31.4** (note: the version bump *past* the 3.31.3 in issue #282 — 3.31.4 registers `gemma4`/`gemma4_unified` in the `LLMTypeRegistry`) loads and runs **dense Gemma 4 E4B 4-bit** (`mlx-community/gemma-4-e4b-it-4bit`, ~4.8 GB) **stock — no registration shim** — and **Qwen3.5-4B-MLX-4bit**, both text-only. On 2 real meetings × 2 models: **100% citation validity (0 fabricated `@ref`)**, template-conformant, correctly speaker-attributed. Perf: Gemma-E4B ~32 tok/s short / ~21 tok/s long, load ~2.6 s warm. We stayed on the dense E2B/E4B tier and never touched the #282-broken variants (assistant/MTP, 26B MoE, 31B dense). **Two carry-forward gotchas for the Phase-3 port:** (1) set `additionalContext: ["enable_thinking": false]` for Qwen3.x-family (else chain-of-thought leaks into the report); (2) the ship build must use **`xcodebuild`** with the **Metal Toolchain** component provisioned (bare `swift build` produces a binary with no `.metallib` → runtime "Failed to load default metallib"), and the `@main` entry file must **not** be named `main.swift`. *S1 is CLOSED — GO* (Paul, 2026-07-16: "if MLX Qwen3.5-4B won, I'm good to leverage that"). Runtime + the 9-meeting automated bake-off are both GO; **no human blind A/B is required** — MLX Qwen3.5-4B is the on-device summary default and `llama-helper` retires at Phase 3.4. (See Decisions.)

**S1 quality bake-off — 9 meetings, all 3 backends 9/9 (2026-07-16):**

| Backend | Citation validity | Owner attribution | Grounded names | Gen latency | Peak RAM |
|---|---|---|---|---|---|
| Qwen GGUF (current baseline) | 96.1% | 96.4% (n=28) | 91.3% | 33.2 s wall | — |
| **MLX Qwen3.5-4B-4bit** | **96.7%** | **100% (n=40)** | 91.3% | 27.4 s | 2.37 GB |
| Gemma 4 E4B-4bit | **100%** | 90.9% (n=11) | 82.6% | 22.8 s | 2.56 GB |

Advisory `claude -p` blind judge (NOT the gate, n=9): MLX-Qwen vs baseline **5–4 (even)**; Gemma-E4B vs baseline **1–8 (baseline preferred)**. **Read:** **MLX-Qwen matches the GGUF baseline** on every quality axis, faster, at 2.37 GB — a clean drop-in for the current model *in the Swift path* → **desktop default GO**. **Gemma-E4B** is citation-perfect and fastest but scores lower on owner-attribution/name-grounding and the judge clearly preferred the Qwen outputs — so on the current prompt it's **not** a stronger desktop summary; keep it as the **mobile-tier** candidate (128K ctx, fits a phone) rather than the desktop default. Note MLX-Qwen's 2.37 GB peak *also* fits modern iPhones, so the desktop winner may double as the mobile model — simplifying the tier story (settle at Phase 6). Caveats: MLX sampling can't replicate llama.cpp's `presence_penalty`/`repeat_penalty` (documented asymmetry); Gemma's attribution n=11 is small; the judge is one advisory model over 9 meetings.

**Exit:** S1–S3 decide *whether each engine goes Swift or stays sidecar-bridged*. **S4 (CloudKit/store sync) is no longer a Phase-0 gate** — it moves to **Phase 5.5** as real infrastructure (revised 2026-07-16: sync machinery is deferred until the mobile app is explored and the Developer account is bought). What Phase 0 *does* still owe the store is confirming **SQLiteData** as the load-bearing choice on its **local** merits (real SQLite → FTS5/sqlite-vec recall survives) — the CloudKit-sync half of S4 is validated later, in Phase 5.5. A miss on S1/S2 doesn't kill the migration — it means that engine stays a Rust sidecar longer while the rest proceeds. **Status (2026-07-17): S1 GO (MLX-Qwen), S2 GO-with-caveats (SpeechTranscriber), S3 CONDITIONAL-GO (FluidAudio + calendar count hint; sherpa fallback) — the only Phase-0 item not formally closed is S3, and it's blocked purely on data (need one human-verified 3+ speaker recording), not on the engine.** Because diarization is Phase-3 step 5 (last) and sherpa stays callable as fallback, this does **not** block starting the migration. **Net: Phase 0 is effectively cleared to proceed to Phase 1.5/3 (store-first); S3's formal close is a later, low-risk confirmation once a clean multi-speaker recording exists.**

## Phase 1 — ~~Sync-ready store foundation~~ → collapsed (revised 2026-07-16)

**This is no longer a standalone early phase.** Under the revised sequencing (principle 4), the CloudKit *machinery* moves late (**Phase 5.5**, after the full Mac app, before mobile) and its only early obligation — **schema-sync-awareness** — folds into the store port in **Phase 3** (step 1). Rationale for the move: Paul is staying **local-only for now** (no Apple Developer account until the mobile app is actively explored), and iCloud sync serves the mobile story he hasn't committed to. Sync still has standalone Mac-to-Mac / backup value, so it stays in the plan — just switched on when the account is bought, not before.

What was Phase 1 splits cleanly:
- **Schema-shape decisions (cheap now, expensive to retrofit) → Phase 3, "Sync-aware schema" checklist.** Built into the Swift store from day one even though sync is off, so Phase 5.5 is a switch-on, not a data migration on live meetings.
- **Sync machinery (cheap to defer, touches no data) → Phase 5.5.** Account, iCloud container + entitlement, SQLiteData CloudKit wiring, S4-as-real-infra, conflict testing, first backfill.

The old exit criterion ("store is sync-capable, a future mobile client attaches with no schema surgery") is preserved — it's just *reached* at Phase 5.5 on a schema that was kept sync-ready since Phase 3, rather than built cloud-connected up front. **Owner still resolved: the Swift store owns CloudKit** (no interim `cloudkit-helper` sidecar; sync cursors/state persist via the DB owner per principle 3) — that holds whenever 5.5 lands.

## Phase 1.5 — Engine extraction (the seam the whole plan hangs on)

*New in v2 — the v1 plan never answered "where does the Rust engine live once Swift owns the window." Original answer: a headless `ari-engine` daemon the Swift shell spawns, extracted first inside the Rust tree with tests green. **Revised 2026-07-17:** the crate extraction happened, but the daemon + cross-process flip (Stages C/D) are DEFERRED in favor of native-first — see § Where we are now.*

**Stage B1 COMPLETE (2026-07-17) — the `ari-engine` crate is carved and Tauri-free.** Done as a strangler (decouple-in-place → extract crate), fully green + reviewed; detail in `docs/plans/ari-engine-carve.md`. What landed:
- **`ari-protocol` crate** — the NDJSON wire envelope (request/response/event/stream, `WireError`, `StreamEvent`) byte-exact to the spec; 13 round-trip tests.
- **`engine/` decoupling seams** — `EventSink` trait (+`TauriEventSink`, `event_sink()` owned variant), `Paths` (resolved once at startup), and the `Engine` context that collapses the deferred-init `AppState` + 5 managed states into one `Arc<Engine>` (non-generic; notification manager deliberately excluded as a future host capability). Wired live into `run()` behavior-neutrally.
- **~102 commands across 14 services migrated** off `tauri::State<AppState>`/manager-states onto `&Engine` (pure `*_impl(&Engine)` + thin `#[tauri::command]` shim; names/args unchanged so `generate_handler!` + frontend IPC are identical): onboarding, persons, meeting_series, diarization, meeting_time, summary(+templates+summary_engine), calendar, recall(conversations/embed_models/commands/stream), api.rs (meetings/settings/recall-answer — recall safety shell preserved verbatim), parallel_commands. **Zero `AppState`/manager-state consumers remain.** Providers never used AppState.

**Stage A of the carve is now functionally COMPLETE** (detail + progress log in `docs/plans/ari-engine-carve.md`):
- **✅ Emit sweep done** — every engine-logic `app.emit(...)` (whisper/parakeet/ollama/apple, the full audio pipeline incl. the deep recording-task helpers, database, calendar-sync) routes through `engine.events()`/`event_sink()`. Only genuinely host-side emitters remain by design (`tray`, `notch/bridge`, and `TauriEventSink` itself).
- **✅ State retirement done** — all manager states + the dead `SystemAudioDetectorState` retired, and **`AppState` fully removed**: the `Engine` is the single DB owner, every command + startup/shutdown path reaches DB/sub-state via `&Engine`. Only the Runtime-generic `NotificationManagerState` stays host-managed (the future `Notifier` seam).
- Runtime-validated: record→transcribe→summarize passes confirmed in the signed app (2026-07-17), including after the audio hot-path emit conversion.

**Host-capability seams (resolved during the carve):** the three things a headless crate can't hold — `Notifier` (notifications), the Tauri store plugin, and `app_config_dir` paths — were resolved as the crate carved: engine-side JSON stores (`onboarding-status.json`, `recording_preferences.json`) moved to a `Paths`-based home; host-only stores stayed host (`tray` menu-bar, `notch` `showNotch`); the dead HTTP-era `get_auth_token` was deleted; and `Notifier`/`EventSink`/`Paths` became injected seams (Tauri impls host-side).

**Command-surface audit (settled 2026-07-17):** 284 registered / 210 live / 74 deletable / 51 event channels (~2× the v2 estimate). The 37 vestigial group was pruned (36 removed + committed, `cargo check` green — analytics no-ops, whisper parallel-processing, HTTP-era); the 37 unwired-dead group is deferred to a per-command re-verify pass (bundle the dead `api_*_profile` trio + its orphaned `make_api_request`/`APP_SERVER_URL` HTTP subsystem). Full map: `docs/plans/engine-extraction.md`.

**DEFERRED — Stages C/D/E:** the method router (`dispatch`), the NDJSON daemon + cross-process flip, and the end-to-end regression gate. The go-forward is **Swift-native, not a daemon** (see Decisions / § Where we are now); `engine-extraction.md` stays the reference for that eventual protocol if/when Swift needs to drive a Rust subsystem it can't yet replace.

**Exit (revised):** the `ari-engine` crate is a clean, Tauri-free, tested foundation. Rather than stand up a daemon for a Swift shell to drive, AriKit layers are built standalone Swift-side (Models/Store/Recall done or underway) while the Rust/Tauri app stays the frozen shipping baseline until each subsystem is replaced.

## Phase 2 — Native macOS shell (the pivot)

Stand up the **native macOS SwiftUI app** as the new host. It spawns `ari-engine` (which still spawns/owns the existing sidecars) and talks the Phase-1.5 protocol.

> **PROGRESS (2026-07-21):** The `Ari/` Xcode app target exists and is the go-forward host. **S0 skeleton ✅**, **"import existing library" ✅** (S8-lite, below), the **native read UI ✅** (S6 — `docs/plans/arikit-native-read-ui.md`, six slices, reviewed), and the **shell/navigation + Marginalia design arc ✅** — app icon + branding, Home landing + rich left-nav rail, Bricolage Grotesque display face, Marginalia component library + design gallery, macOS 26 Liquid Glass chrome, and shared list/row components (eight commits `b8e4dd9`→`1f623d1`; see `docs/plans/arikit-component-library.md`, `docs/plans/liquid-glass-adoption.md`, `docs/plans/marginalia-review-fixes.md`). Detail plan for the shell + capture: `docs/plans/arikit-native-shell.md`. **Read-UI deepening ✅ (2026-07-21):** the meeting-detail read-pane redesign (resizable two-pane; listen-back / referenced-moments / source-record panels; `MarginaliaMarkdownView` block rendering with inline `[MM:SS]` citation chips; per-series summaries) landed, **and** the Series screens were rebuilt — detail as the "connected record" (rich ledger with cross-meeting `@mref` citation chips that open the cited member meeting at its moment, via a new `MeetingMoment` nav value + `MeetingDetailView(initialSeek:)`), and the list made searchable + alphabetically sorted with real count/last-meeting rows (new `SeriesSummary` DTO + `SeriesRepository.allSummaries()` aggregate). See the STATUS/Post-S6 block in `docs/plans/arikit-native-read-ui.md`. Still to do in Phase 2: EventKit calendar (S7), then the capture/recording vertical (3.2 S1–S5, engine Phase 3).

**Revised from v1 — native-first, scoped WebView.** v1 hosted the *entire* React UI in a WKWebView behind a full bridge; that bridge would carry ~136 `invoke()` call sites across 39 files, an event push channel used by 25+ files, *and* a byte-range asset protocol — all 100% throwaway. Instead:

- **Read UI goes native on day one**: meetings list, meeting details (read view), people, series. These are the easiest screens — and they are the **seed of the mobile app's UI in Phase 6** (build them well here; the reuse arrow is Mac → mobile). All native SwiftUI is themed **Marginalia from `brand/tokens.json` from the first screen** — the Arivo theme never gets ported to Swift (see Phase 4 for the full design-system spec). **UI direction confirmed 2026-07-20:** native-first with Marginalia as a *thin accent* (not a reskin), a **frameless/unified title bar** (no top border), an aligned **button system** (4 roles × 2 sizes), and **SF-Symbol icons — never emoji, never in navigation** (`docs/plans/arikit-native-shell.md §10`; see also the `swift-ui-direction` memory).
- **A scoped WKWebView hosts only the genuinely-hard screens**: the BlockNote editor panel, settings, and the new-meeting/recording flow — bridging only the **~30–40 commands those screens actually use**, enumerated explicitly before work starts. The bridge needs three pieces, all scoped: (i) `invoke` adapter via `webkit.messageHandlers`; (ii) event push via `evaluateJavaScript` callbacks; (iii) a `WKURLSchemeHandler` with **byte-range support** replacing `convertFileSrc` for audio playback/scrubbing (used by `AudioPlaybackContext.tsx`).
- Native window, menu bar, notifications, tray, and the **notch UI** absorbed (retire the `ari-notch` sidecar's *UI*; note its scheduler/state logic lives in Rust `notch/bridge.rs` and ports with the engine in Phase 3 — the Phase-2 absorption is the panel, not the brain).
- **EventKit calendar moves into the shell now** — `calendar/eventkit.rs` is already native-API code via objc2, it's the cheapest early Swift win, and it's the most TCC-sensitive subsystem, benefiting most from a real bundle identity.
- **Distribution posture pinned: Developer ID signing, hardened runtime, NO App Sandbox** (consistent with personal-use scope; sandboxing would break process-tap audio capture and sidecar model directories; CloudKit works fine under Developer ID once the paid account exists — but that account is **not needed for Phase 2**, only for Phase 5.5 sync). Through Phase 5, the app runs local-only on the existing self-signed `Ari Dev Signing` identity (or a fresh self-signed one) exactly like the Tauri app does today — **no Apple Developer account required to build, run, or use the full Mac app locally.** New code identity means **one-time re-grant of Mic/Screen Recording/Calendar TCC permissions** — expected, document it, done. The Developer ID cert + notarization become relevant only if/when distribution widens (still an open personal decision).
- **Data continuity milestone: "import existing library."** A one-shot migrator that adopts the live app-data dir (`~/Library/Application Support/com.meetily.ai`): SQLite DB (26 migrations of real data), meeting folders + audio files, crash-recovery checkpoints, and the multi-GB downloaded models (Parakeet ONNX, GGUF, embedders) — with post-import verification counts. Settings/API keys: audit where they live (DB vs Keychain); Keychain items are tied to the signing identity/team and can be orphaned by the change.

**Exit:** the app is a Swift host with native OS integration, calendar, and a native read UI, running **local-only (no CloudKit yet — that's Phase 5.5)**. **Per the confirmed native-first decision (2026-07-20) there is NO WebView bridge** — the shell is fully native; the block editor's advanced block-editing is the sole holdout, decided on evidence at Phase 4 (`arikit-native-shell.md §10`). Tauri and Next.js are deleted (the earlier "scoped WebView / Next.js as a guest in three screens" plan is retired).

## Phase 3 — Engine migration (behind the shell)

Replace `ari-engine` modules with Swift behind the same protocol, each gated on its Phase-0 spike bar. **Revised order — the store moves first** (v1 had it 4th, which forced every earlier Swift piece to write through Rust or open a second WAL connection; the store is the dependency hub and migrating the hub last-but-one was backwards).

1. **Store** — `sqlx` SQLite → **plain GRDB** (decided 2026-07-17, revising the 2026-07-16 SQLiteData pick — see Decisions; SQLiteData v1.7.0 proved a `@Table`/structured-queries paradigm with ~8 heavy transitive deps and no benefit until CloudKit at 5.5, and it rests on GRDB anyway, so GRDB keeps identical semantics; revisit SQLiteData-vs-hand-rolled-sync at 5.5). Port the schema, freeze sqlx migrations at cutover, and port the **hybrid-retrieval recall engine + its safety-shell tests** (loopback-only, bounded context, no invented citations — dual-run per principle 2). Includes the multi-backend embedder plumbing (Apple NLEmbedding default / nomic GGUF / Ollama). From here on, the Rust remainder reads/writes via the Swift store over the engine protocol — **one owner, always**. Build the store **sync-aware but sync-off** (principle 4): honor the checklist below so Phase 5.5 flips CloudKit on without a schema migration.

   **Why GRDB — and never SwiftData** *(the invariant is the recall differentiator; the GRDB-vs-SQLiteData choice was settled 2026-07-17)*: the "most Apple-native" store is **SwiftData**, but it deliberately **hides the SQLite file** — no raw SQL, no `FTS5`, no loadable extensions like **`sqlite-vec`**. Ari's recall/"Ask my meetings" differentiator (F7) *is* hybrid FTS5 ⊕ vector search over raw SQL, so SwiftData would **amputate the feature the product is built around** — it stays permanently rejected. Both remaining candidates are **real SQLite**: **plain GRDB** and **Point-Free SQLiteData** (which is itself built on GRDB, adding a `@Table`/structured-queries DSL + first-class CloudKit sync). The Store is built on **plain GRDB now** — SQLiteData v1.7.0 proved a different record paradigm (not a superset of plain `FetchableRecord`/`PersistableRecord`) with ~8 heavy transitive deps and no benefit until CloudKit lands at Phase 5.5; since it rests on GRDB anyway, starting on GRDB keeps identical semantics and defers the SQLiteData-vs-hand-rolled-sync question to 5.5. The recall port carries straight over either way (the Swift mirror of "DB access through repositories only"). Full reasoning: `docs/plans/arikit-store.md §0.1(3)`.

   **Sync-aware schema checklist** (the four cheap-now / expensive-later decisions — audit finding 2026-07-16: the live schema is already ~70% there):
   - **Stable string/UUID primary keys — already the case.** Every table in the current schema uses `TEXT PRIMARY KEY` (`meeting-…` UUIDs); **zero autoincrement-integer PKs** anywhere (verified across all 25 migrations incl. `persons`/`speakers`/`profile_facts`). CloudKit keys records by stable global IDs; keep this in the Swift models — do **not** regress to `Int` autoincrement because it feels idiomatic.
   - **Nullable-or-defaulted columns on any synced table.** SQLiteData's CloudKit layer needs this (a record can materialize on device B before its related records arrive). Free if designed in from the start; a live-data migration if relaxed later.
   - **Soft-delete tombstones, not hard deletes** — *the one real gap today* (no `deleted_at`/`is_deleted` in the current schema). Sync needs a delete to propagate rather than resurrect from the other device. Add tombstone columns to the fresh synced tables now; retrofitting after real deletes are gone is lossy.
   - **Per-record conflict granularity** — CloudKit resolves conflicts per record, so don't over-denormalize a synced table into one wide row (an edit clobbers more on conflict). Normal normalization suffices; just don't widen for read convenience.
   - (Plus the standing split from principle 5: **text tables sync, audio stays device-local** — already the shape; the `add_audio_sync_fields` migration keeps audio as file paths, not blobs in the meeting row.)
2. **Capture + encode** — `cidre`/`cpal` → Core Audio process tap (`CATapDescription` + `AudioHardwareCreateProcessTap`, public API since macOS 14.4 — the same API cidre wraps, so the wrapper literally vanishes) + AVAudioEngine mic + **AVFoundation/AVAudioFile AAC encode replacing the ffmpeg sidecar** (ffmpeg does encoding, mixing, decoding, *and* crash-recovery remux today — all four duties must be replaced, then the sidecar deleted). Known AVAudioEngine gotchas (budget for them; this step is effort-parity with cpal, not free): `installTap` must use the hardware format or it crashes; resample to 48 kHz mono via `AVAudioConverter`; hardware format changes at runtime on device switches (AirPods at 16/24 kHz) — the same device-churn handling `device_monitor.rs` does today. Port VAD (`SpeechDetector` or a small CoreML silero), the incremental saver/crash recovery, import + retranscription, and the notch scheduler.
3. **STT** — ✅ **COMPLETE (2026-07-20).** Whisper/Parakeet → SpeechAnalyzer/**SpeechTranscriber** (adopted per S2; **WhisperKit deferred**, protocol kept backend-ready). Landed as the capture-agnostic `AriKit/Sources/AriKit/Engine/STT/` layer: a segment-emitting `TranscriptionProvider` protocol (the Swift mirror of the Rust trait), `SpeechTranscriberProvider` (verified recorded-file path + designed live-stream path), `SpeechAssetManager` (absorbs apple-helper `EnsureAssets`/`Probe` in-process; the model-download manager largely **dissolves** since SpeechAnalyzer assets are OS-managed), `TranscriptMapping` (segment → `Models.Transcript`), `STTLocale`/`CMTimeMapping`. Gate PASSED: product `SpeechTranscriberProvider` mean core WER **0.2345 ≤ Parakeet 0.2814** over the 5 S2 meetings (reproduces the spike), 100% word-timestamp coverage, punctuation on every meeting; No-Fake-State + Swift 6 strict; 427 tests/62 suites green. The **live path awaits its real mic feed from Phase 3.2** (designed + shape/cancellation-tested here, not runtime-verified). Plan: `docs/plans/arikit-stt.md`. Already proven once in `apple-helper`. **Live-transcription port reference:** Apple's `RecognizingSpeechInLiveAudio` sample (downloaded to `~/Downloads/RecognizingSpeechInLiveAudio`) is the canonical pattern for Ari's live path — `DictationTranscriber(.Preset.progressiveLongDictation)` + `SpeechAnalyzer(modules:)` + `analyzer.analyzeSequence(inputs)`, streaming `transcriber.results` (volatile→final, replace by overlapping `audioTimeRange`), `AssetInventory.assetInstallationRequest(supporting:)` for on-demand model install, and an optional custom LM via `contentHints: [.customizedLanguage(...)]`. Wire the mic input as an `AsyncSequence<AnalyzerInput>` (the sample uses an AVCaptureSession provider; Ari would tap AVAudioEngine + the Core Audio process tap).
4. **Summary** — llama-helper (llama.cpp) → **MLX via `mlx-swift-lm`** / FoundationModels (short contexts only); keep cloud providers (URLSession) and the Claude CLI provider (`Process`). Retire the `llama-helper` sidecar when MLX passes S1. Port the SummaryContext assembly, templates, citations verification (`citations.rs`), and **persons extraction/reconciliation + series detection** engines.
5. **Diarization / re-ID** — sherpa-onnx (`diarize-helper`) → FluidAudio (CoreML, **offline pipeline**, `OfflineDiarizerManager`). Port the tuned post-merge/floor recipe; this is the hardest step and is scheduled late deliberately. Keep the sherpa sidecar callable as fallback until FluidAudio hits parity on the S3 set. **S3-driven design requirement (2026-07-17): drive FluidAudio with a speaker-count hint from the calendar attendee list.** At its default *auto* speaker count FluidAudio collapses multi-speaker mixed audio to a single speaker (disqualifying); feeding `clustering.numSpeakers` (or a tight `min/maxSpeakers` band) recovers near-sherpa parity — this is the F4→F1 pipeline (EventKit attendees → the diarizer's expected-speaker count) and must be wired, not optional. Prefer an *exact* count when the room is known; fall back to a bounded range otherwise (a loose 2–10 band over-splits). Also apply a **speech-time floor** to drop spurious tiny singletons (mirrors the sherpa `postprocess.rs` floor). License note: FluidAudio SDK is Apache-2.0 but its pyannote-derived weights are **CC-BY-4.0** (attribution — only matters if distribution ever widens).

**Exit:** all heavy work runs in Swift; the `llama-helper`, `diarize-helper`, and `ffmpeg` sidecars are deleted; the `ari-engine` Rust daemon shrinks to nothing and is retired.

## Phase 4 — UI nativization

Replace the remaining web screens with SwiftUI, driving each from `AriKit` directly. **The real route list is 11, not 6** (v1 undercounted by ~45%): `/`, `/meetings`, `/meeting-details`, `/new-meeting`, `/chat`, `/settings`, **`/calendar`, `/people`, `/person-details`, `/series`, `/series-details`** — though the Phase-2 native read UI already covers several.

**The design system is decided: "Marginalia"** (adopted 2026-07-16, canonical in **`brand/`** — `BRAND.md` + `tokens.json` v1.2.0 + `assets/*.svg`; presentation layer in the Brand Book artifact). The Arivo theme (root `DESIGN.md`/`DESIGN.json`, Signal Desk) does **not** carry over — it remains the enforced, test-fixture system for the Tauri app only, until the web UI is deleted.

**UI direction confirmed (2026-07-20, Paul — reacting to a direction mockup):** the Swift UI is **native-first**; Marginalia is a **thin accent layer, not a reskin** — lean on native macOS components (`NavigationSplitView`, system list rows, materials, SF type, SF Symbols) and add brand only as accent: Shin-kai navy as the sole interactive ink (≤8%, selection/links/citations/speaker names), Iron Gall navy headings, warm-cream grounds, recording red for live capture only. A **frameless/unified title bar** (no top border). A **button system**: 4 roles — Primary (accent solid, one per view) / Secondary (tonal) / Quiet (text) / Recording (red, capture-only) — × **2 sizes only** (26 px default, 32 px large). **Icons/SF Symbols, never emoji — never an emoji in navigation** (hard rule). The ported visual-system parity test asserts brand *tokens* are used, not that every surface is custom-styled. Full detail + direction mockup: `docs/plans/arikit-native-shell.md §10`; captured in the `swift-ui-direction` memory. What the Swift UI builds against:
- **Tokens:** `brand/tokens.json` drives the SwiftUI theme (asset-catalog colors, spacing 4–48, radii 6/10/14) and a ported visual-system test replaces the Arivo one. Two-inks color system: Shin-kai accent `#1B3A8C`/`#7E9BE8` under the ≤8% Signal Rule; Iron Gall `#152C66` heading ink (light) / paper-white (dark). **✅ Already scaffolded (2026-07-17, parallel to Phase 1.5):** `AriKit/Sources/AriKit/DesignSystem/` ports the full token set to Swift — `MarginaliaColor` (13 roles × light/dark), `MarginaliaSpacing`/`MarginaliaRadius`, `MarginaliaTypography` (9-style ramp; Bricolage ≥17pt via `Font.custom`, SF Pro/SF Mono via `Font.system`), `MarginaliaMotion`, `MarginaliaRules`, and a `.marginaliaTextStyle(_:in:)` view modifier. The "ported visual-system test" also exists: `MarginaliaTokenParityTests` (Swift Testing) walks up to `brand/tokens.json` and asserts Swift↔JSON parity for colors, spacing, radii, the full ramp (incl. ink/tracking/uppercase), and rule constants — so the two can't silently drift.
- **Type:** SF Pro body/UI; **Bricolage Grotesque** (OFL) bundled and used for headings **≥17pt only** (SF Pro Semibold below) — same self-hosted pattern as Space Grotesk today. `Font.custom` + `relativeTo:` for Dynamic Type.
- **Marks:** one drawing everywhere — the R2 "Dictation" gesture as app icon (squircle, paper field) and wordmark; its 16px "signature flick" cut is the menu-bar/notch recording glyph (`brand/assets/`). The old Arivo crescent-ring icon is retired.
- **Carried-over principles** (now brand-wide per `BRAND.md` §"Where the rules apply"): No-Fake-State, two inks, sentence case, warm neutrals, consent voice. "Animate only real state" is the product-surface motion rule.
- **Build-time tuning items** from the brand doc: selection-wash alpha (test 16–18% light mode on a bright display), wordmark re-export with outlined Bricolage once bundled.

- Migrate simplest remaining routes first; leave the **block editor last**. Native path: `TextEditor` + `AttributedString` (macOS 26) covers rich text; the block-editing layer (drag handles, slash menus, `@ref` badge decorations) either gets a native rebuild on that foundation or that one panel stays a scoped WebView longer. Audio playback goes native (`AVPlayer` — no more URL-scheme bridge).
- **MCP extensibility (F8)** lands here (or as the one in-flight feature earlier, Swift-native per principle 8): an MCP server exposing the meeting store binds to `AriKit.Store`/`Recall` — build it against AriKit once, not against Rust first.
- Retire the scoped WKWebView as the last panel lands.

**Exit:** no web UI remains; Next.js, React, and the bridge are removed.

## Phase 5 — Convergence & cleanup

- Fold the Mac engine's models into the shared `AriKit` package so Mac and iOS share one definition (the sync-aware schema converges here — see Phase 3's checklist; CloudKit is still off at this point). **✅ Domain value-type layer already ported (2026-07-17, parallel to Phase 1.5):** `AriKit/Sources/AriKit/Models/` now holds the 10 shared domain types (`Meeting`, `Transcript`, `Speaker`, `SpeakerSegment`, `Person`, `ProfileFact`+`ProfileFactSource`, `Series`, `CalendarEvent`+`Attendee`) mirroring the Rust `database`/`persons`/`meeting_series`/`calendar` models, plus a support layer (phantom-typed `Identifier<T>`, forward-tolerant `.unknown(String)` enums, an RFC3339 date strategy, `LocalAudioReference` for the results/audio split). Swift 6 strict-concurrency clean (all `Sendable` value types, no persistence deps — `Store`/GRDB is still Phase-3.1), architect→implement→review complete, `swift test` green (40 tests / 9 suites). Plan + settled design decisions: `docs/plans/arikit-models.md`. Two known follow-ons deferred to the Store port: a snake→camel decode adapter for the four database-origin types (their Rust structs have no `serde(rename_all)`, so the engine wire is snake_case), and `Series`' dropped stored columns (`owner_person_id`, own timestamps). `Summary` intentionally not ported (no Rust row — a Store-port decision).
- Delete the entire Rust tree (`ari-engine`/`frontend/src-tauri` remnants, `llama-helper`, `diarize-helper`) and the `frontend/` web app.
- Delete the root Arivo `DESIGN.md`/`DESIGN.json` along with the web UI they governed — `brand/` is the sole design source of truth from here on (its visual-system test equivalent runs against `brand/tokens.json`). Update all `.claude/` docs and the PRD to describe the Swift architecture. Archive this plan.

**Exit:** one Swift codebase, one design system, full Apple-native macOS integration — and an `AriKit` package whose engine, store, and recall are all Swift, running **local-only** on a **sync-ready-but-off** store (the sync layer is wired but not activated until Phase 5.5). **The Mac app is now "done" in the local sense** — a complete, finished Mac product with no cloud dependency. This is the natural stopping point if the mobile app is never pursued.

## Phase 5.5 — Turn iCloud sync on (the machinery, when the account is bought)

*Deferred here from the old Phase 1 (revised 2026-07-16). Starts when Paul decides to actively explore the mobile app — **it is the gate between a finished local Mac app and mobile**, and the first point that needs the paid Apple Developer account.* Because Phase 3 built the store **sync-aware**, this is a **switch-on, not a schema surgery**.

- **Get the Apple Developer Program membership ($99/yr)** — the only hard external prerequisite; it's the entitlement key for CloudKit/iCloud (data lives in the *owner's own private iCloud DB*, per-Apple-ID, developer-invisible — not a shared/public store). Also unlocks the CloudKit Console (record/schema inspector) and APNs (CloudKit change pushes ride on it).
- **Create the iCloud container + entitlement**, bind the SQLiteData store to it, and **activate CloudKit sync** on the already-sync-aware synced tables (text only; audio stays device-local per principle 5). **Owner: the Swift store owns CloudKit** — no `cloudkit-helper` sidecar; sync cursors/state persist via the DB owner (principle 3).
- **Promote spike S4 to real infrastructure — validate the roundtrip without a mobile client**: Mac → iCloud → a second Mac instance (or a second SQLiteData store on the same private DB) proves records sync, offline changes queue, and conflicts resolve (record-level). Confirm raw-SQL recall (FTS5 / sqlite-vec) still runs against the synced DB.
- **One-time backfill** of existing local meetings into iCloud — a clean upload precisely *because* the schema was kept sync-ready since Phase 3 (no migrate-live-data step first). Leave the local DB authoritative; verify counts after upload.

**Exit:** the finished Mac app now syncs its text results through the owner's private iCloud — valuable on its own (Mac-to-Mac / backup-restore) and the proven sync layer Phase 6 attaches to. Audio never leaves the device that recorded it.

## Phase 6 — Ari Lite (the mobile app)

*Only starts once Phase 5.5 is complete (principle 4) — i.e. once sync is proven on the finished Mac app.* A standalone iOS/iPadOS app target on the finished `AriKit`, aiming for **the same feature set as the Mac app minus speaker identification**. Not a read-only viewer — it records, transcribes, and summarizes on-device, and syncs text results with the Mac through the CloudKit layer stood up in Phase 5.5.

- **Reuse, don't rebuild.** Link `AriKit` (Models / Store / Recall / Context / Engine) directly; the shared engine already compiles for iOS. Seed the SwiftUI screens from the Mac's Phase-2 read UI + Phase-4 native screens, adapted to touch/compact layouts and themed from `brand/tokens.json`.
- **What ships:** recording (mic-first), STT (SpeechAnalyzer/WhisperKit), summarization, calendar (EventKit), recall/Ask, person profiles, series ledgers — all synced bidirectionally (text) with the Mac.
- **What's excluded / open (from the target-architecture deltas):**
  - **Speaker ID (F1)** — excluded by design; no proven on-device mobile diarization model. Revisit if FluidAudio/CoreML pyannote proves viable on iPhone/iPad or a lighter model appears. Transcripts sync from the Mac *with* speaker labels; meetings recorded on the phone have none until (if ever) opened/re-processed on a Mac.
  - **System-audio / far-side capture** — no process-tap equivalent on iOS; scope the mic-first vs call-capture question at kickoff.
  - **On-device summary model** — pick the mobile default (Gemma 4 E2B/E4B via MLX-swift, both 128K ctx, vs cloud) based on real iOS memory headroom; FoundationModels is fallback-only. On-device mobile summary is viable per the tiered strategy (see S1 / Decisions).
- **Preserve the invariants** on the new target too: recall safety shell, consent-before-record, No-Fake-State — as the ported Swift test suites, run in the iOS target.

**Exit:** a meeting recorded on either device appears (text) on the other within seconds; the mobile app is a full meeting-intelligence tool minus speaker labels. This is the multi-device product family the whole migration was for — delivered *after* a solid, finished Mac app rather than racing it.

## Subsystem migration map

*(Expanded in v2 — the v1 map missed six live subsystems and one sidecar.)*

| Today | Swift replacement | Wrapper fate | Risk | Phase |
|---|---|---|---|---|
| `cidre` system audio | Core Audio process tap (macOS 14.4+ public API) | **Vanishes** (Rust-only artifact) | Low | 3.2 |
| `cpal` mic + device_monitor | AVAudioEngine + AVAudioConverter (format-churn handling required) | **Vanishes** | Low-Med | 3.2 |
| **`ffmpeg` sidecar** (encode/mix/decode/recovery) | AVFoundation / AVAudioFile / AudioToolbox | Replaced; sidecar deleted | Med | 3.2 |
| `silero` VAD | `SpeechDetector` / small CoreML | Mostly subsumed | Low | 3.2 |
| incremental_saver crash recovery | Port to Swift (AVFoundation remux) | Native | Med | 3.2 |
| import / retranscription | Port to Swift | Native | Low | 3.2 |
| whisper.cpp / Parakeet | SpeechAnalyzer / **SpeechTranscriber** (WhisperKit deferred) | Replaced by framework | ✅ **DONE 3.3** (gate PASSED: WER 0.2345 ≤ Parakeet 0.2814) | 3.3 |
| model-download manager | Partly dissolves (OS-managed assets) + `SpeechAssetManager` | ✅ **DONE 3.3** (dissolved to `SpeechAssetManager`; HF/model-dir lifecycle gone) | Low | 3.3 |
| llama.cpp (`llama-helper`) | MLX (`mlx-swift-lm`) / FoundationModels (4k ctx — short tasks only) | Replaced; sidecar deleted | Med (quality) | 3.4 (gate S1) |
| Cloud LLM providers | URLSession | Trivial | Low | 3.4 |
| **Claude CLI provider** | `Process` subprocess (not HTTP) | Native, macOS-only | Low | 3.4 |
| persons extraction / reconciliation | Port to Swift (LLM-backed, rides summary engine) | Native | Med | 3.4 |
| series detection + ledgers | Port to Swift | Native | Low | 3.4 |
| citations verify/snap (`citations.rs`) | Port to Swift + tests | Native | Med | 3.4 |
| sherpa-onnx (`diarize-helper`) | FluidAudio (CoreML pyannote, offline; weights CC-BY-4.0) — **must be fed a calendar-derived speaker-count hint** (S3 finding) | Replaced; sidecar deleted | **High → Med** (S3 conditional-GO: near-parity w/ count hint, ~20–60× faster; sherpa fallback until formal close) | 3.5 (gate S3) |
| `sqlx` SQLite + repositories | GRDB (SQLiteData revisit at 5.5) + CloudKit | Native | Med | **3.1** |
| Hybrid recall + safety shell + embedders | Port to Swift, invariants as ported tests | Native | Med | **3.1** |
| **EventKit calendar sync** (`calendar/`) | EventKit direct (already native-API via objc2) | Native — **cheap early win** | Low | **2** |
| notifications / tray / **onboarding** | UserNotifications / MenuBarExtra / native flow | Native | Low | 2 |
| `apple-helper` (STT/FM probe) | Absorbed in-process | ✅ **STT half DONE 3.3** (`Transcribe`/`EnsureAssets`/`Probe` absorbed into `Engine/STT`); FM summarize absorbed in 3.4 | Low | 3.3 |
| `ari-notch` UI / scheduler | UI absorbed in shell / scheduler ports with engine | Split: 2 / 3.2 | Low | 2 + 3.2 |
| audio playback (asset protocol) | Bridge: `WKURLSchemeHandler` w/ byte-ranges → later native `AVPlayer` | Retired | Med | 2 → 4 |
| **MCP server (F8, committed feature)** | Build once against `AriKit` | Net-new | Med | 4 (or Swift-first earlier) |
| Next.js / React / BlockNote UI (11 routes) | SwiftUI; block editor last | Rewritten | **High (effort)** | 2 (read UI) + 4 |
| Tauri host + IPC (~140 cmds → ~90 live) | Phase-1.5 engine protocol → native calls | Retired | Med | 1.5 → 2 |
| **Existing user data + models** (26 migrations, multi-GB models) | One-shot "import existing library" migrator + verification | Net-new | Med | 2 |
| — (new) | CloudKit results/sync layer (infra, no client yet) | Net-new | Low | 1 |
| — (new) | **Ari Lite iOS/iPadOS app** (full engine minus speaker ID) | Net-new | Med (new target, platform deltas) | **6** |

## Risks & mitigations

- **STT/summary quality regression** → Phase-0 gates S1/S2 with the *committed* eval harness (P0); if a gate misses, that engine stays a Rust sidecar under the Swift shell until MLX/Apple catches up (the shell supports both via the engine protocol). S2 in particular is unproven — public data only shows SpeechAnalyzer beating Whisper-Small.
- **Diarization is the riskiest port** → scheduled last; offline-pipeline comparison only; sherpa sidecar stays callable until FluidAudio hits parity.
- **Two-writer DB corruption** → principle 3 (single owner) + store migrates first in Phase 3. Never open the SQLite file from two ORMs across processes.
- **~~Bridge scope creep~~ (retired 2026-07-20)** → native-first is confirmed; **no WebView bridge is built.** The block editor's advanced editing is the only deferred screen, its treatment decided on evidence at Phase 4 (`arikit-native-shell.md §10`).
- **Rewrite outpaced by feature work** → principle 8 WIP limits + feature-freeze scope: finish F1–F4 on Rust, freeze, build new features Swift-side. Kill any phase that hasn't shipped user-visible value in ~6 weeks and re-plan.
- **CloudKit quota / audio bloat** → sync text only (counts against the *user's* iCloud quota, not developer billing); audio stays local.
- **Data loss at cutover** → the Phase-2 import milestone with verification counts; old app-data dir left untouched until the user confirms.
- **"Rewrite that never ships"** → each phase from 2 on has a standalone user-visible Mac payoff (Phase 2 ships native shell + calendar + read UI; Phase 3 ships per-engine; Phase 4 ships native screens). Phases 1 and 1.5 are the scaffolding phases (sync store + engine extraction) — justified because they de-risk the store hub and the single riskiest cutover (host swap) and are done in known languages with tests green. **The multi-device dividend moves to the end (Phase 6) by decision** — mobile follows a finished Mac app rather than racing it.
- **Mobile scope creep / platform gaps** → the mobile app is explicitly *later* (Phase 6) and its hard deltas (no iOS system-audio tap, tighter MLX memory, no speaker-ID model) are flagged now so the shared engine is designed for them, but *resolved* only at Phase-6 kickoff — not allowed to pull scope into the Mac migration.
- **Latest-OS floor** → accepted and documented; revisit only if distribution scope (Q6) ever widens beyond personal use.

## Decisions

**Resolved during validation (2026-07-16):**
- **Apple Developer account ($99/yr)** — confirmed genuinely required for CloudKit/iCloud entitlements (not available to free accounts). **Needed before Phase 5.5** (was "before Phase 1"; sync deferred 2026-07-16). **Not** needed for Phases 0–5 — the full local Mac app builds and runs on a self-signed identity, no account. It's the entitlement key for CloudKit only; the data itself lives in the *owner's own private iCloud*, developer-invisible.
- **Distribution posture** — Developer ID + hardened runtime, **no App Sandbox** (sandbox would break process-tap capture and sidecar model dirs; personal-use scope doesn't need MAS). Revisit only with Q6.
- **Where the Rust engine lives during transition** — the eventual design is a headless `ari-engine` daemon behind a versioned protocol, spawned by whichever host is current. **The daemon (carve Stages C/D) is now DEFERRED** (revised 2026-07-17): the Phase-0 spikes greenlit Swift-native STT/summary/diarization, so the go-forward is native-first and each AriKit layer is built standalone (Store owns its own file + a one-time importer, no cross-process protocol needed). Build the daemon later and narrowly, only for whatever Swift genuinely can't do yet. The crate (`ari-engine`) already exists and is Tauri-free; only the router+daemon+flip are deferred.
- **Parakeet licensing** — settled: `parakeet-tdt-0.6b-v3` is CC-BY-4.0 (attribution only). The old "separate NeMo terms must be verified" concern in product.md is stale; update it.

**Resolved 2026-07-16 (Paul):**
- **Feature freeze is *now*.** The current Rust/Tauri app works and is the frozen baseline — all of F1–F8 as they stand today ship on Rust. There is no "which features finish on Rust" list: the line is today. Going forward is Swift-first. *(Was open Q5 + Q4.)*
- **No parallel Phase 1 push; Swift *is* the single go-forward track.** Paul keeps using the working Rust product through the migration. The WIP slot is the Swift migration itself — not a Rust product still under active feature development. New features go Swift-first; the Rust app gets **reactive maintenance only**, and even a fix should be done in Swift when it reasonably can be. *(Was open Q4.)*
- **Store strategy: plain GRDB now, SQLiteData revisit at Phase 5.5** — **revised 2026-07-17**, superseding the 2026-07-16 SQLiteData reaffirmation after the implementer verified SQLiteData v1.7.0's actual API. **SwiftData stays rejected** — it hides the SQLite file (no raw SQL, no FTS5, no loadable extensions like sqlite-vec), which would amputate Ari's recall/Ask differentiator (F7 hybrid FTS5 ⊕ vector search, BM25⊕vector RRF); that constraint is invariant. The remaining choice was SQLiteData-vs-plain-GRDB, both **real SQLite**. **GRDB won for the foundation slice:** SQLiteData v1.7.0 turned out to be a `@Table` + `swift-structured-queries` paradigm (not a superset of plain `FetchableRecord`/`PersistableRecord` records) carrying ~8 heavy transitive deps, with **no benefit until CloudKit lands at 5.5** — and since SQLiteData itself rests on GRDB ~7.6, starting on GRDB keeps identical semantics while deferring the SQLiteData-vs-hand-rolled-sync decision to 5.5 (the old "fallback if sync sours" clause, promoted to the starting point). Store built on `groue/GRDB.swift`; the recall port still carries straight over (the Swift mirror of the repositories-only rule). Full reasoning: `docs/plans/arikit-store.md §0.1(3)` + `.claude/rules/swift-conventions.md`. *(Was open Q1; revised.)*
- **CloudKit publisher: the Swift store owns CloudKit** — no interim `cloudkit-helper` sidecar. Since Swift is the single go-forward track, there's no case for writing sync/conflict logic twice. (Applies whenever sync lands — now **Phase 5.5**, not Phase 1.) *(Was open Q3.)*

**Resolved 2026-07-16 (Paul) — mobile app direction:**
- **Mac first, then sync, then mobile.** The mobile ("lite") app is a **separate, later project that starts only once the macOS Swift migration is complete *and* iCloud sync is turned on (Phase 5.5 done)** — built on the finished `AriKit`, not shipped early as a multi-device dividend. The mobile app is Phase 6. The reuse arrow points **Mac → mobile** (the Mac's native UI seeds the mobile UI).
- **Mobile scope = full app minus speaker ID.** "Lite" means *without speaker identification (F1)*, **not** read-only. The mobile app records, transcribes, and summarizes on-device — the same feature set as the Mac — except on-device speaker diarization/re-ID, for which no proven mobile model exists yet. Revisit inclusion if one appears. (Other genuine platform gaps — no iOS system-audio process tap; tighter on-device summary-model memory — are flagged in the target architecture and resolved at Phase-6 kickoff.)
- **Sync machinery deferred to Phase 5.5; only *schema-awareness* builds early. (Revised 2026-07-16 — supersedes "sync infrastructure builds early anyway.")** Paul is **local-only for now** and buys the Apple Developer account only when actively exploring mobile. So the CloudKit *machinery* (container, sync wiring, S4-as-infra, backfill) moves to Phase 5.5, after a finished local Mac app. What stays early is the cheap insurance: the Phase-3 store is built **sync-aware but sync-off** (UUID PKs — already the case; nullable/defaulted synced columns; soft-delete tombstones; per-record conflict granularity; text-syncs/audio-local), so Phase 5.5 is a switch-on, not a live-data migration. Sync also earns its keep independent of mobile (Mac-to-Mac / backup). Full split in the Phase 1 (collapsed) and Phase 5.5 sections.

**Resolved 2026-07-16 (Paul) — summary engine:** **S1 CLOSED → GO on MLX Qwen3.5-4B (4-bit) as the on-device summary default.** The 9-meeting bake-off showed MLX-Qwen matches the shipped GGUF baseline on every quality axis (96.7% vs 96.1% citation validity, 100% vs 96.4% owner attribution, equal name-grounding), faster (27.4 s vs 33.2 s), at 2.37 GB, and the Swift-runtime spike ran it stock in `mlx-swift-lm` 3.31.4 with 100% citation validity. Paul: "if MLX Qwen3.5-4B won, I'm good to leverage that." → the `llama-helper`/llama.cpp sidecar retires at Phase 3. Gemma-4-E4B stays a **mobile-tier** candidate (citation-clean, 128K ctx, but judged weaker on substance — not the desktop default); note MLX-Qwen's 2.37 GB also fits iPhones, so it may serve both tiers (settle at Phase 6). FoundationModels remains the zero-download floor only. Cloud providers stay regardless.

**Superseded framing (kept for context) — tiered on-device strategy:** model scales to device capability — not one model everywhere. The summary engine is an abstraction with per-tier model selection: desktop (RAM headroom) runs the largest model that passes quality — MLX Qwen-4B or **Gemma 4 E4B** (128K ctx), with room to go bigger; mobile runs a mobile-grade on-device model — **Gemma 4 E2B (~1.5 GB) / E4B (~5 GB), both 128K ctx via MLX-swift** — which makes robust *on-device* mobile summarization viable (it is NOT a mobile weak point; only diarization/F1 is). FoundationModels is the zero-download floor for short content, not the default. This supersedes the earlier "FoundationModels or cloud may be the mobile default" note.

## Development tooling — Claude-Code-driven Swift

Ari is built entirely with Claude Code from the CLI (no human in Xcode); the Swift tree must be set up for that from day one. Sources reviewed 2026-07-16: [keskinonur/claude-code-ios-dev-guide](https://github.com/keskinonur/claude-code-ios-dev-guide) (793★, unlicensed — copy patterns, not text; snapshot from Jan 2026, verify tool names against current releases) and [johnrogers/claude-swift-engineering](https://github.com/johnrogers/claude-swift-engineering) (220★, MIT — safe to vendor skill content; TCA-opinionated, filter accordingly).

Adopt when the Swift tree is created (Phase 1 / 1.5):

1. **XcodeBuildMCP in `.mcp.json`** (`INCREMENTAL_BUILDS_ENABLED=true`, `XCODEBUILDMCP_DYNAMIC_TOOLS=true`) — typed build/test/clean, simulator boot/install/launch, log capture and screenshots (the agent can *see* the running app), and `swift_package_build`/`swift_package_test` for AriKit + `build_device_proj` for the macOS target. Highest-value single item.
2. **CLAUDE.md discipline ported to Swift:** root CLAUDE.md pinning Swift 6 strict concurrency, `@Observable`-MVVM (we are NOT adopting TCA unless decided otherwise), GRDB-only persistence (the Swift mirror of today's "repositories-only" rule), and min-deployment (macOS/iOS 26); plus scoped `CLAUDE.md` per AriKit feature module — same pattern as today's `frontend/` and `src-tauri/` scoped files.
3. **Hooks:** PostToolUse SwiftLint/SwiftFormat on `*.swift` edits; session-start check reporting Swift toolchain + booted-simulator state.
4. **Skills/commands translated:** `/build`, `/run-app` (build + launch the signed macOS `.app` — the analog of today's `app:local`), `/test` (`swift_package_test` + `xcodebuild test`), `/implement-feature` reading `docs/specs/`.
5. **Agent set, model-stratified** (pattern from claude-swift-engineering): a plan-only swift-architect writing `docs/plans/<feature>.md`, implementation agents, a swift-code-reviewer geared to Swift 6 concurrency migration; vendor its **GRDB and SQLite skills** (MIT) as project skills for the Store port. Skip its TCA agents.

## Sequencing summary

```
        ┌─ LOCAL-ONLY — no Apple Developer account needed ────────────────────────────┐
Phase 0  │ P0 rigs ✅ + S1 GO / S2 GO / S3 cond-GO ── COMPLETE; S4-local folds into 3.1 │
Phase 1  │ (collapsed)                         ── sync machinery → 5.5; schema-aware→P3 │
Phase 1.5│ Extract headless ari-engine ✅ B1     ── brain carved (7 commits); C/D DEFERRED │
Phase 2  │ Native SwiftUI shell                ── fully native UI (no WebView bridge)    │
         │                                       + EventKit + notch UI + data import    │
Phase 3  │ Engine → Swift, store FIRST         ── GRDB store built SYNC-AWARE but        │
         │                                       SYNC-OFF; recall → capture → STT →     │
         │                                       summary/persons/series → diarization   │
Phase 4  │ Remaining UI → SwiftUI (11 routes)  ── block editor last; MCP (F8) on AriKit │
Phase 5  │ Converge & delete Rust              ── Mac app "done" LOCALLY: one Swift pkg  │
        └──────────────────────────────────────────────────────────────────────────────┘
─────────────────────────────────────────────  ← finished local Mac app; below needs the account
Phase 5.5  Turn iCloud sync ON                 ── buy account + container; S4-as-infra; backfill
─────────────────────────────────────────────  ← sync proven; mobile starts here
Phase 6    Ari Lite (iOS/iPadOS)               ── full engine MINUS speaker ID, on finished AriKit
```

**Immediate next step:** see **§ Where we are now** at the top of this doc — it is the single, current source of truth for status and the active front (kept there so status lives in one place, not duplicated at both ends of the plan).

## Revision history

Condensed for provenance; the body above always reflects the current plan.

- **v8 (2026-07-21)** — Phase 2 shell/navigation + Marginalia design arc landed (app icon/branding, Home + left-nav rail, Bricolage face, component library + gallery, macOS 26 Liquid Glass, shared list/row components; eight commits `b8e4dd9`→`1f623d1`). Status/Current-front + Phase 2 PROGRESS updated to mark shell/branding done; new Phase-2 UI sub-plans linked (`arikit-native-read-ui`, `arikit-component-library`, `liquid-glass-adoption`, `marginalia-review-fixes`); current focus moved to the (in-flight, uncommitted) meeting-detail read-pane redesign.
- **v7 (2026-07-17)** — AriKit Store (Phase 3.1) COMPLETE + Recall Slice 1 landed (see § Where we are now). Bottom "immediate next step" consolidated into the top status; the five stacked version-notes collapsed into this history.
- **v6 (2026-07-17)** — B1 engine-brain carve COMPLETE; pivot to Swift (Store-first); carve Stages C/D (daemon) DEFERRED, native-first.
- **v5 (2026-07-17)** — Phase 0 complete / go-forward confirmed (S1/S2/S3 GO); Phase 1.5 carve underway; AriKit design-system + Models scaffolding landed.
- **v4 (2026-07-16)** — sync-sequencing revision: stay local-only; iCloud/CloudKit sync moved from Phase 1 to a new Phase 5.5 (after the finished local Mac app, before mobile); buy the Apple Developer account only then. Store built sync-aware-but-off in Phase 3.
- **v3 (2026-07-16)** — Mac migrates/finishes first; mobile "lite" is a separate later Phase 6 (full app minus speaker ID, not read-only). *(v3 had CloudKit landing early — superseded by v4.)*
- **v2 (2026-07-16)** — framework fact-checks, codebase-grounding audit, architecture critique, and the engine-extraction seam design (retained in the body).
- **v1 (2026-07-16)** — initial strangler-migration proposal.

Store-library note: the 2026-07-16 "SQLiteData decided" call (v4) was **revised to plain GRDB on 2026-07-17** after the implementer verified SQLiteData v1.7.0's actual API; SwiftData stays permanently rejected. Full reasoning in Decisions + `docs/plans/arikit-store.md §0.1(3)`.
